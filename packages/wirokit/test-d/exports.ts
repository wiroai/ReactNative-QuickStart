import { WIROKIT_VERSION, WiroKitInfo } from '@wiro-ai/wirokit-react-native';

const version: '0.1.0' = WIROKIT_VERSION;
const infoVersion: '0.1.0' = WiroKitInfo.version;

void version;
void infoVersion;
