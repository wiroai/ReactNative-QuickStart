/** The package version shared with runtime headers and diagnostics. */
export const WIROKIT_VERSION = '0.1.0' as const;

/** Package-level information for WiroKit React Native. */
export const WiroKitInfo = Object.freeze({
  version: WIROKIT_VERSION,
});
